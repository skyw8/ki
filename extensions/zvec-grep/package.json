{
  "name": "ki-zvec-grep",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "bun install && bun run build",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "test": "bun run build && bun test"
  },
  "dependencies": {
    "@zvec/zvec-grep": "0.2.1"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2",
    "vite": "^6.0.0"
  }
}
