# Rust build
/target/

# Prebuilt sidecar binary (build with: cargo build --release && cp target/release/freerouter bin/freerouter)
/bin/freerouter
/bin/freerouter.exe

# Local / editor
*.swp
*~
.DS_Store
